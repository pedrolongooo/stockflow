{
  "name": "stockflow-estoque",
  "version": "1.0.0",
  "description": "Gerenciamento de Estoque — Projeto DevOps",
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/tests/**/*.test.js"],
    "coverageReporters": ["text", "lcov", "cobertura"],
    "coverageDirectory": "coverage"
  }
}